== 이어받는 명령 ==
git fetch origin
git switch feat/scheduled-publishing-and-diversity
git pull
npm run dev

== Decision D-040 기록 — Approval Source Trust and Corroboration ==

2026-08-15 변경 기록. 승인준비 출처 기준을 공식 도메인 필수 방식에서
출처 신뢰 경로 방식으로 변경한다.

- AI가 실제로 참고한 URL은 반드시 저장한다.
- URL만으로 통과하지 않고 Fetch 성공과 원고 핵심 내용의 실질적 일치를 확인한다.
- 공식·1차 출처는 다른 출처 없이 단독 검증을 허용한다.
- 비공식·2차 출처는 독립된 다른 출처에서 같은 핵심 내용이 확인될 때만
  `system_verified`로 승격한다.
- 정보 기준일과 독자 노출 최종 검토일은 승인 차단 조건에서 제외한다.
- 일반 Claim 전체를 필수 차단 Claim으로 만들지 않고, 중대한 미지원·충돌
  Claim만 품질 또는 승인 정책 차단 대상으로 유지한다.

수정 대상: `08_DECISION_LOG.md`, 승인 출처 정책 문서,
`ApprovalEvidenceVerification`, `ApprovalReadiness` 및 회귀 테스트.

== Decision D-041 기록 — Generated Citation URL Preservation ==

원고 본문·메타데이터에 AI가 실제로 작성한 HTTPS 출처 URL을 생성 단계에서
Evidence 후보로 병합한다. Provider web-search 진단에 URL이 빠져도 원고가
사용한 출처를 검증하며, URL만으로 통과시키지 않고 기존 Fetch·Claim 일치·교차
확인 규칙을 그대로 적용한다.

수정 대상: `core/ai/AIWorkflow.ts`, `08_DECISION_LOG.md`.

== Decision D-042 기록 — AdSense 승인용 WordPress 공개 예약 허용 ==

`adsense_approval` 콘텐츠도 `wordpressSchedulePublicPublish`가 명시적으로 켜져
있고 현재 Revision의 품질·승인준비·권한 검사와 예약별 최종 확인을 통과하면
WordPress `future` 예약을 사용할 수 있도록 변경한다. 즉시 공개 발행
`publicPublish`는 계속 비활성화한다.

수정 대상: WordPress 예약 서비스·설정 UI·Decision Log·제품 문서·회귀 테스트.

== Decision D-043 기록 — 정보 기준일 위치 및 출처 안내 분리 ==

다음 신규 원고부터 `정보 기준일: YYYY-MM-DD`는 제목 아래·서론·일반 본문에
생성하지 않고, 결론 뒤 별도 `정보 기준과 다시 확인할 곳` 영역에 한 번만
생성한다. 공식 서비스·페이지를 다시 확인하라는 문장은 출처 원문이 아닌
독자 안내이므로 별도 문단으로 분리하고, 검증된 출처 링크와 시스템 검토일은
시스템 영역으로 분리한다. 기존 임시저장 원고는 수정하지 않는다.

수정 대상: `core/ai/AIWorkflow.ts`, `core/approval/ApprovalPolicy.ts`, 관련 테스트.

npm install 은 이전 브랜치에서 뒤처져 있을 때만. 이번 작업은 package.json 을
건드리지 않았다.

이번 세션 커밋은 origin 에 push 하지 않았다. 커밋·푸시는 CLAUDE.md/AGENTS.md
원칙상 사용자가 지시할 때만 한다. 사장님이 "이상이 없다면 알아서 커밋해"라고
지시하셔서 커밋만 했다.

== 검증 완료 (2026-08-14) ==

사장님이 실행한 결과 커밋 3def960 은 전부 통과했다.

  npx tsc --noEmit   → 출력 없음 (클린)
  npx eslint .       → 출력 없음 (클린)
  npx vitest run     → Test Files 323 passed (323) / Tests 2015 passed (2015)

직전 실행이 18 failed / 1997 passed 였으므로 18건이 전부 해소됐다.
Phase 0 의 코드·테스트는 종결이다. 남은 완료 조건은 **실제 원고 1편 생성**뿐이다.

참고로 커밋 당시에는 이 3개를 실행하지 못했다. 그 환경에서
- 브리지 리눅스 VM: node_modules 가 Windows 설치본이라 rolldown 네이티브
  바인딩 로드 실패
- 클라우드 컨테이너: npm registry 가 403 으로 막힘 (vitest, typescript 포함)
때문에 러너 자체를 띄울 수 없었다.

대신 한 검증:
- 변경된 6개 파일 전부 `node --check` 구문 통과
- duration 규칙을 실제 저장 원고에 시뮬레이션 → 기존 테스트 기대 11건 중 10건
  충족, 실제 원고 오검출 0건
- 재작성한 테스트 단언을 손으로 추적 검증

검증이 끝났으므로 되돌릴 이유는 없다.

== 현재 상태 (2026-08-14) ==

브랜치 feat/scheduled-publishing-and-diversity, HEAD = 3def960.

전체 진단을 다시 수행했고 결과를 `Docs/diagnosis-20260814/` 에 넣었다.
읽는 순서: 01 진단 → 02 재설계 → 03 계획 → 04 Phase 0 결과.

== 확정된 근본 원인 ==

원고가 깨지는 원인은 개별 버그가 아니다. **완성된 원고에서 문장을 사후에
잘라내는 설계**가 Decision Log D-037 을 우회한 채 들어와 있었다.

D-037 원문: "VERIFY는 실패 시 전체 Generation을 차단하지 않고 같은 Generation
Prompt에서 해당 구체 Claim을 제거하거나 일반화하며" — 즉 **생성 프롬프트 안에서**
해결하라는 결정이다. Decision Log(D-001~D-038) 어디에도 사후 삭제를 승인한
결정이 없다.

실측 (`.bright-studio/studio-data.json`, 콘텐츠 52편):
- 인벤토리 48항목 중 43개(90%) 철회. 그중 38개가
  `unreported_generated_critical` — `applyGeneratedFactualClaimInventory`
  안에서 `disposition: "removed"` 가 상수로 박혀 있고 합성 claimId 라
  `verifiedCriticalClaimIds` 에 절대 들어갈 수 없다. 게이트가 아니라 삭제기였다.
- 최근 15편 1,499문장 중 금액·비율·날짜·자격 조건이 남은 문장 6개.
- 삭제된 것은 사실 주장이 아니라 글의 리드 문장·체크리스트·주의 고지였다.
  `criticalSurfacePattern` 이 `우대 조건`, `자격 요건` 같은 **값 없는 명사구**를
  잡는데, 그게 이 사이트의 주제어다.
- 8/10 이후 발행 0건. 8/11~14 에 8편 생성, 입력 54만 토큰 소모.

교착 구조: "고위험 사실" 정의가 3곳에 따로 있고 서로 모순이었다.
- `GeneratedFactualClaimInventory.criticalSurfacePattern` — 조건/자격 단어를
  잡고 기간은 못 잡음 (삭제 담당)
- `GeneratedClaimBinding.detectHighRiskScalarTokens` — 기간을 잡고 조건어는
  못 잡음 (차단 담당)
→ 삭제된 것은 요구된 적 없고, 차단된 것은 지울 수단이 없었다. 품질 100점
원고가 "2년" 한 단어로 영구 차단되고 재생성으로도 안 풀렸다.

지난 세션들이 고친 버그 6종(표 셀 공백, `12,0` 잘린 값, 문단 통째 삭제, 빈 표
껍데기, 고지 오삭제, dangling ID)이 전부 이 하나의 파생물이다.

== 승인된 결정: D-039 (Decision Log 등록 필요) ==

사장님이 D-039 와 Phase 0~5 전부를 승인하셨다. **아직 08_DECISION_LOG.md 에
등록하지 않았다. Phase 5 작업이다.**

D-039 Write-time Fact Constraint 요지:
- 생성된 canonical 원고는 어떤 자동 단계도 수정하지 않는다.
- 외부 검증이 필요한 사실의 정의는 저장소에 하나만 둔다. 수치값·단위·귀속
  주체가 모두 있을 때만 external_fact.
- 모든 발행 차단 사유는 실행 가능한 해결 경로를 함께 제시해야 한다.
- AI 호출은 Generation 1회 + Quality Review 1회 유지, Source Preflight 를
  세 번째 호출로 명시하되 Claim 지문 기준 프로젝트 캐시 적용.

== 이번 세션에 한 일 (Phase 0) ==

`3def960 fix: stop withdrawing sentences from the finished manuscript`

1. `core/approval/GeneratedFactualClaimInventory.ts`
   - 명시 인벤토리와 untracked 스윕의 `removeGeneratedFactualSurface` 호출을
     모두 제거. 이 단계는 이제 **기록만** 한다. `let document` → `const`.
   - 스윕 허용 목록을 `retained CRITICAL만` → **보고된 모든 표면**으로 변경.
     삭제를 멈추니 보고됐지만 미검증인 문장이 스윕 시점에 여전히 존재해서
     `unreported_generated_critical` 로 **한 번 더 집계**되고 있었다. 같은
     문장에 진짜 claimId 와 합성 claimId 가 둘 다 붙는 상태였다. 테스트
     "removes reported and unreported new CRITICAL surfaces" 가 2개 기대에
     3개가 나와서 발견했다.
   - `pruneLongFormStructure` 는 무해한 no-op 가드로 남겨 뒀다 (Phase 5 제거).
   - `disposition: "removed"` 이름은 그대로 뒀다. Phase 1 에서 `unsupported`
     로 개명한다. 소비자 2곳(`optionalEvidenceCheck`,
     `deliberatelyRemovedGeneratedFactualClaimIds`)이 이미 "본문에 문장이
     남아 있으면 철회로 세지 않는다"라서 거짓 보고는 생기지 않는다.

2. `core/approval/GeneratedClaimBinding.ts`
   - `recencyWindowSpans` 신설. `최근`/`지난` + 기간을 duration 검출에서
     면제한다. 임계 수식어(이내/이상/이하/미만/초과/안에/내에/까지)가 뒤따르면
     면제를 취소한다.
   - **처음에 이걸 잘못 잡았다.** "기한 수식어가 붙은 것만 검출"로 좁혔더니
     `계약 기간은 24개월로 정해져 있습니다`, `주거 지원 기간은 24개월입니다`
     같은 진짜 제도 조건까지 풀렸다. 기존 테스트가 잡아냈다. 실제 원고에서
     문제였던 건 `최근 1년 동안 발생한 소득을 적습니다` 류 **독자 기록 안내**
     뿐이었으므로 그만큼만 면제하도록 다시 좁혔다.

3. 테스트 재작성
   - 삭제 계약 9개 → 보존 계약으로.
   - 안전 삭제를 검증하던 3개(인접 숫자 훼손, 빈 표 처리)는
     `removeGeneratedFactualSurface` **직접 호출**로 옮겨 살렸다. 이 함수는
     Phase 4 복구 도구가 쓴다.
   - `"detects an approximated period, which no source can support"` 를
     `"exempts a recency window the reader is asked to look back over"` 로
     교체했다. 원래 테스트의 전제("근사 기간은 출처가 없으니 차단")가 발행을
     멈춘 논리 그 자체다. 교체 근거를 주석에 남겼다. 이 판단이 틀렸다고
     보시면 되돌리면 된다.

== Phase 0 을 계획서와 다르게 한 것 ==

계획서 0-4 "앵커 소실 차단을 advisory 로 완화"는 **하지 않았다.**
앵커가 사라지던 원인이 스윕이었고 그게 없어졌으니 새 원고에서는 발생하지
않는다. 이미 훼손된 원고의 앵커 소실은 Phase 4 복구로 원문을 되살려 푸는 게
맞다. 측정 근거 없이 정합성 검사를 약화하는 것은 AGENTS.md 14장이 금지한다.
→ 0-4 는 Phase 4 로 옮겼다.

== Phase 1 완료 (2026-08-14) ==

커밋 18e4581 + cd6ebd1. 검증 통과: tsc 클린 / eslint 클린 /
324파일 2,024테스트 전부 통과.

core/approval/FactualSurfaceTaxonomy.ts 신설 — "출처가 필요한 사실"의 정의가
저장소에 하나만 존재한다. 인벤토리 스윕이 criticalSurfacePattern 대신 이걸
호출한다.

판정 규칙: 값(단위 포함) + 귀속 주체가 값 바로 옆(전후 22자)에 있을 때만
external_fact. 값이 없으면 editorial_frame. 값은 있는데 주체 불명이면
unattributed_value — 사장님 판정에 따라 권고이지 차단이 아니다.
정보 기준일/최종 검토일은 publication_meta 로 분류 제외.
최근/지난 + 기간은 억제하되 임계 수식어가 붙으면 억제 취소.

48건 재분류: editorial_frame 38 / unattributed_value 5 / external_fact 3 /
publication_meta 2.

진행하며 배운 것:
- 표 셀은 자기 주체를 못 담는다. 캡션·열 헤더·행 라벨을 함께 봐야 한다.
- 귀속 주체는 값 앞뿐 아니라 뒤도 봐야 한다. 한국어는 구문에 따라 주체 위치가
  바뀐다 (주거 지원 기간은 24개월 / 6개월 이내에 폐업한 사업자만 신청).
- 기간·기준·조건은 lookbehind 필수. 장기간·판단기준 안의 부분일치가 문장을
  통째로 사실 주장으로 만든다.
- 계획서의 3-3(가정 예시 면제)은 하지 않았다. 기존 게이트가 "가정 안의 법정
  기간도 검출한다"를 테스트로 고정해 뒀고 뒤집을 근거가 없다. 해당 사례는
  unattributed_value 로 떨어져 권고 처리된다.

**detectHighRiskScalarTokens 는 아직 taxonomy 로 배선하지 않았다.** 그쪽의
차단/권고 분리는 Phase 3 의 PublishReadinessVerdict 가 생겨야 성립한다.

== Phase 0·1 실측 검증 통과 (2026-08-14) ==

개발 서버에서 원고 1편을 실제로 생성해 판정했다.
대상: content-mst1wpt0-w4xtjm "대출 상환 방식 비교로 내 월 납입과 만기 부담
가르는 법". 주제를 일부러 이걸로 골랐다 — 예전에 표 15칸 중 14칸이 비어 나온
바로 그 주제다(content-mss9s98k).

  기록 14건 / 사라진 것 0건      (이전: 14건 전부 삭제)
  표 15칸 / 빈칸 0               (이전: 14칸 공백, 남은 하나는 "12,0")
  dangling 참조 0
  본문 5,074자 / 문단 30개       (이전 훼손본: 1,157자)
  정보 기준일·확인 경로 본문에 있음

판정 스크립트: node scripts/verify-generated-article.mjs [contentId]
상세: Docs/diagnosis-20260814/06_VERIFICATION_RESULT.md

새 분류 실측: 후보 113개 중 external_fact 11개(전부 표의 실제 금액),
editorial_frame 101, publication_meta 1. 우대 조건·중도상환 조건 같은 주제어는
하나도 걸리지 않았다.

**남은 문제 2가지 (둘 다 새 결함이 아니라 미착수 단계의 문제):**

1. 10자 부족으로 발행이 막힌다 — Phase 3 대상.
   CONTENT_SECTION_PROSE_INSUFFICIENT / 섹션 "대출 실행 전 비교표를 만드는
   순서" / minimum 250 actual 240. 5,074자 원고가 한 섹션 10자 부족으로
   in_review 다. Claim 게이트가 아니라 구조 진단이므로 교착은 아니지만,
   화면이 무엇을 고쳐야 하는지 말해 주지 않는다. PublishReadinessVerdict 가
   { kind: "edit_body", blockId, hint } 를 붙여야 한다.

2. 계산 예시 고지가 면제 3요소에 못 미친다 — Phase 2 대상.
   표의 금액 11건이 external_fact 로 잡혔는데 실제로는 계산 예시다.
   생성이 쓴 고지는 "…동일한 가정을 둔 계산 예시입니다"까지고,
   "실제 계약 청구액을 대신하지 않습니다" 류의 한계 고지가 빠졌다.
   기준을 낮출 문제가 아니라 생성이 고지를 완전하게 쓰게 만들 문제다.

== Phase 2 착수분 완료 + 원고 ready 도달 (2026-08-14) ==

커밋 11b8642 + 31c2e7b. 검증: tsc 클린 / eslint 클린 / 325파일 2,028테스트 통과.

**원고가 ready 에 도달했다. 이번 사이클 처음이다.**
content-mst2yzkz-77mroq "생활비 예산 배분 계산 방법으로 내 월급의 쓸 돈과 모을
돈 정하기". 품질 100 / approved=true / 차단 사유 없음 / 승인 준비 15건 미통과 0.

계획서가 틀린 부분이 있었다. 생성 프롬프트는 이미 손대져 있었다 —
withApprovalSourcePreflightInstruction 이 preflight 검증값과 출처를 주입하고,
approvalInformationDateContract 가 "정보 기준일: YYYY-MM-DD" 형식과 재확인 경로
표기를 강제한다. todo.txt 의 "생성 프롬프트는 손대지 않았다"는 사실이 아니었다.

실측으로 확인된 진짜 빈 구멍은 좁았다: **계산 예시 고지가 반쪽이었다.**
생성이 "…가정을 둔 계산 예시입니다"까지 쓰고 "실제를 대신하지 않는다"를 빼서,
표의 금액 11건이 전부 출처 없는 외부 주장으로 기록됐다.

withCalculationExampleDisclosureContract 추가. 프롬프트가 제시하는 모델 문장을
calculationDisclosureContract(분류기가 실제로 검사하는 계약)에서 가져오므로
프롬프트와 분류기가 서로 다른 문장을 말할 수 없다. 테스트가 이걸 고정한다.

효과 (같은 세션, 계산 주제 2편 비교):
  1편(적용 전): external_fact 11 / 인벤토리 14건 / blocked / in_review
  2편(적용 후): illustrative 6, external_fact 0 / 인벤토리 0건 / approved / ready

== 이번에 배운 것 (반복 금지) ==

**core/ai/index.ts 에 AIWorkflow 를 값으로 export 하면 앱이 죽는다.**
그 barrel 은 원래 타입만 재export 했다. 값 export 하나가 AIWorkflow →
../approval → ApprovalSourceDocumentServerAdapter(server-only) 를 클라이언트
번들로 끌어들여 빌드가 깨진다.
tsc·eslint·vitest 는 전부 통과한 상태였다. 번들러 경계는 앱을 띄워야 드러난다.
→ 테스트는 core/ai/AIWorkflow 에서 직접 import 한다.
→ **원고 1편 생성이 완료 기준인 이유가 이것이다. 초록 스위트로 대체 불가.**

== Phase 3 최우선 목록 (해결 경로 없는 차단들) ==

1. **Source Preflight 실패에 진단이 안 남는다.**
   content-mst2r5wl-eadjnz "전월세 전환율 계산 방법" 이 preflight 에서 막혔는데
   approvalSourcePreflightDiagnostic 이 {} 다. 어느 Claim 이 왜 막혔는지 알 수
   없고 사용자에게는 해결 경로 없는 실패만 남는다.
   지난 세션이 월세 세액공제 건에서 "진단 구조는 잘 작동했다"고 적었지만 이
   경로에서는 아무것도 저장되지 않는다.
2. **10자 부족 차단.** content-mst1wpt0-w4xtjm 이 CONTENT_SECTION_PROSE_
   INSUFFICIENT(minimum 250 / actual 240) 하나로 in_review 다. 5,074자 원고가
   한 섹션 10자 때문에 막히는데 화면이 무엇을 고치라고 말해 주지 않는다.
3. **Site Readiness 게이트가 페이지별 noindex 를 검사하지 않는다.**
   2026-08-14 실측: 게이트가 15건 전부 통과라고 보고했는데, 같은 시점 Google
   Search Console 은 /about/, /disclaimer/, http:// 루트 3개가 NOINDEX 로
   제외됐다고 보고했다. 게이트의 검사 항목은 홈페이지의 전체 차단 신호
   (검색 제외 설정, robots 전체 차단)만 본다. AdSense 가 요구하는 신뢰 페이지가
   개별 noindex 여도 "준비 완료"로 보고된다.
   → 사이트맵에 오르는 페이지의 meta robots 와 X-Robots-Tag 를 개별로 확인해야 한다.
4. PublishReadinessVerdict 신설 — resolution 필수 타입. 위 셋이 첫 대상.

== Phase 3 착수분 완료 (2026-08-14 밤) ==

커밋 1fe6408 + 2fbe2cd + c2b4e46. 검증: tsc·eslint 클린 / 326파일 2,039테스트 통과.

1. **신뢰 페이지를 열어서 확인한다** (1fe6408)
   core/approval/PublicPageIndexability.ts 신설 — meta robots 와 X-Robots-Tag 를
   둘 다 읽는다. Core 에 둔 이유는 티스토리 감사도 같은 답이 필요해서다
   (지금 티스토리엔 noindex 검사가 아예 없다. AGENTS.md 14장 위반이므로 다음 차례).
   WordPress 감사에 trust_page_indexable 추가. 링크만 확인하던 privacy/about/
   contact 와 달리 실제로 페이지를 연다. 안 열리면(404) 그것도 실패.
   SiteApprovalReadiness 검사에 action 필드 추가 — D-039 "차단에는 해결 경로".

2. **감사 수정이 기존 원고에 닿게 한다** (2fbe2cd)
   approvalReadinessInspectionVersion 4.0 → 4.1.
   실측: 15:16 에 검사된 원고가 21:57 에도 그 스냅샷을 그대로 반환했다
   (검사 15개, trust_page_indexable 없음). 실행 신원 키가 일치하면 사이트
   스냅샷을 "비싼 산출물"로 재사용하기 때문이다. 버전을 올린 뒤 같은 호출이
   검사 16개와 새 checkedAt 을 반환했다.
   코드 주석이 이미 이 실패 모드를 알고 있었다 — 지난 세션이 집계에 대해서만
   고치고 사이트 스냅샷은 재사용 목록에 남겨 뒀다.
   ContentMetadata 의 version union 에 "4.1" 추가 (과거 버전은 남긴다).

3. **표가 본문 칸을 뚫고 나가던 문제** (c2b4e46)
   발행본 실측: 렌더러가 figure 에 overflow-x:auto, table 에 min-width:960px 를
   함께 보내는데 워드프레스가 inline style 을 허용 목록으로 걸러 overflow-x 를
   지우고 min-width 만 남긴다. 960px 표가 720px 칸을 넘었다.
   → 워드프레스 표에서 min-width 제거. figure 의 overflow-x 는 유지(무해).
   품질 검사 mobile_table_wrapper_missing 은 내내 통과였다 — 우리가 보낸 HTML 을
   보고, 사고는 도착한 HTML 에서 났다. 이번 사이클 내내 반복된 모양이다.

== 다음 할 일 ==

0. **비교표 열 선별 (표 폭 문제의 근본).** 8열은 블로그 글에 너무 많다.
   실측 예: 구분/고정지출/소득대비비중/비정기적립/배분가능/변동지출한도/저축액/
   남는금액 — "비정기 적립"은 세 행 모두 20만 원(가정값, 표 위 문장에 이미 있음),
   "남는 금액"은 세 행 모두 0원, "소득 대비 비중"은 고정지출에서 바로 계산된다.
   모든 행이 같은 값인 열과 다른 열에서 계산되는 열은 표에 있을 이유가 없다.
   생성 프롬프트와 검증기 양쪽에 규칙을 건다. 데스크톱 폭을 넓히는 것은 해법이
   아니다 — 본문 800px 는 한글 40~45자로 적절하고, 모바일(390px)은 어차피 안 든다.
   테마가 GeneratePress(클래식)라 alignwide 도 지원하지 않는다.
1. **티스토리 감사에 trust_page_indexable 배선.** Core 헬퍼는 이미 있다.
   AGENTS.md 14장 "약한 티스토리 승인 경로 금지".
2. **Source Preflight 실패에 진단이 안 남는다.** content-mst2r5wl-eadjnz
   (전월세 전환율)이 preflight 에서 막혔는데 approvalSourcePreflightDiagnostic 이
   {} 다. 어느 Claim 이 왜 막혔는지 알 수 없다.
3. **10자 부족 차단.** content-mst1wpt0-w4xtjm 이 CONTENT_SECTION_PROSE_
   INSUFFICIENT(minimum 250 / actual 240) 하나로 in_review 다.
4. PublishReadinessVerdict 신설 — resolution 필수 타입. 위 2·3 이 첫 대상.
5. **글 편수.** 애드센스 최대 리스크. 색인된 글이 2편뿐이다(site: 실측).
   예금 적금 비교는 훼손된 1,157자 상태로 발행돼 있으니 복구본으로 재발행할 것.


1. **위 3개 명령 실행 및 실패 정리.** 가장 먼저.
2. **실제 원고 1편 생성.** Phase 0 의 진짜 완료 기준이다. 명령어 통과는
   완료가 아니다. 개발 서버(3000)에서 브라우저로 직접 생성하고
   `studio-data.json` 실측으로 확인한다:
   - 인벤토리가 `removed` 로 기록한 문장이 **본문에 그대로 남아 있는가**
   - 표 빈칸 0개인가
   - `status` 가 `ready` 에 도달하는가
   - 차단이 남았다면 사유가 무엇이고 해결 경로가 있는가
3. ~~Phase 1 — 사실 정의 단일화~~ **완료** (위 참조).
4. **Phase 2 — 생성 시점 제약. 이제 이것이 최우선이다.** — 생성 시점 제약. Preflight 결과를 `VerifiedValuePack` 으로 만들어
   생성 프롬프트에 주입하고, 본문에 출처명과 정보 기준일을 쓰게 한다.
   사장님 방침 "본문에 출처를 최대한 표시한다"의 실제 구현이다. 현재는 게이트
   쪽(`bodyOffersConfirmationPath`)에만 구현돼 있고 프롬프트는 손대지 않았다.
5. Phase 3 — `PublishReadinessVerdict` 로 게이트 통합. `resolution` 필수.
   `app/user-flow` 변경은 반드시 브라우저로 직접 확인할 것.
6. Phase 4 — 결과 기반 테스트(`tests/outcome/`) + 훼손 원고 복구.
   복구 대상(removed > 0): content-msn722jz(18), content-mssubxwf(9),
   content-msrfq4gt(7), content-mslrf8a7(3), content-msolrz90(2),
   content-msosej4k(2), content-mslyfk99(1), content-msou2crv(1).
   `inventory.items[].surfaceText` 와 `locations` 로 재생성 없이 복원 가능하다.
7. Phase 5 — D-039 를 08_DECISION_LOG.md 에 등록, 아키텍처 문서 갱신,
   죽은 방어 코드 제거(`splitsAdjacentNumber`, `rowStillCarriesData`,
   `pruneLongFormStructure` 호출, `fallbackTitle` 인자).

== 미해결로 남은 것 ==

- 월세 세액공제 원고의 `claim_normalization_failed`. 공식 출처
  (law.go.kr, nts.go.kr)를 확보했는데 `normalizeExplicitClaimValue` 가 값을
  못 뽑았다. 진단에 `claim.value` 가 안 남아서 정규화 결함인지 프롬프트
  결함인지 구분하려면 재현이 필요하다.
- `content-mslpwo0c` 에 남은 오탐 1건: "가능하면 연속된 3개월 이상을 비교하는
  편이 낫습니다". 법정 기한이 아니라 글 자신의 권고다. Phase 1 의 귀속 주체
  조건이 걸러낸다. 현재 이 원고는 품질 점수(94)로 in_review 이고 Claim
  게이트로 막힌 적은 없어서 동작 변화는 없다.

== 보류/관찰 중인 항목 ==

- 줄바꿈(CRLF) 정리: `.gitattributes` 가 없어 파일마다 제각각이다. 리눅스에서
  `git status` 를 보면 손대지 않은 파일 87개가 수정된 것처럼 보인다.
  사장님이 "병합 후로 미뤄"라고 지시하셨다. **`git add -A` 를 쓰지 말 것** —
  전부 딸려 들어간다. 파일을 명시해서 add 한다.
  이번 세션에 `AIWorkflowVerifyLifecycle.test.ts` 한 개만 LF 로 맞췄다.
  index 가 이미 LF 라 그래야 내 수정만 diff 에 남는다. 일괄 정규화가 아니다.

== 정리할 것 ==

`_to_delete/` 폴더에 지난 세션 임시 파일(소스 압축본, git stale index.lock)이
들어 있다. 저장소에 필요한 파일은 없으니 폴더째 삭제하면 된다.
`_check/` 는 검증 결과 출력용이며 마찬가지로 추적 대상이 아니다.

== 따라가지 않는 것 ==

`.bright-studio/` 는 git 대상이 아니다. 이 기계의 밝은재테크 Project 에
콘텐츠가 있고 이번 진단은 전부 그 데이터 실측이다. 다른 기계에는 같은
워크스페이스가 없다 — 지난 인수인계가 지목한 content-mss9s98k-mo7oka 등 3편은
**다른 컴퓨터의 워크스페이스**에 있다. 유실이 아니다.
