남은 할 일만 적는다.

작업 규칙·상태 확인법·반복해서 틀린 것은 여기가 아니라
Docs/current/00_FOUNDATION/03_AI_DEVELOPMENT_RULE.md 에 있다.
2026-08-29 이전 기록은 Docs/current/04_DEVELOPMENT/14_WORK_LOG_2026_08.md.

== 이어받는 명령 ==

git fetch origin
git switch feat/scheduled-publishing-and-diversity
git pull
npm run dev

== 지금 바로 ==

1. D-052 확인. 서버 재시작 뒤 근로장려금·국민연금 원고의 발행 화면을 열어
   「함께 읽으면 좋은 글」이 자기 자신이 아닌 다른 글로 바뀌는지 본다.
   (승인·발행 화면을 열면 refresh:true 로 재평가되어 자동으로 고쳐진다.)
   그 다음 근로장려금 원고를 「워드프레스 임시저장」으로 발행해서
   (a) 검증이 통과하는지 (b) 미디어 라이브러리에 새 번호가 안 생기는지 확인.
   발행 전 기준값: post 3784, 미디어 3785, asset db083ffa.
   예약 버튼은 쓰지 않는다 — 예약은 항상 새 글을 만든다.
2. D-053 확인. 본문 시각물(막대·비율·단계·타임라인·비교·수치 타일)이 새 원고
   생성에서 실제로 나오는지 본다. 원고 1편 생성이 완료 기준이다.
   생김새 미리보기: _check/bright-visual-preview.html (렌더러 실제 출력).
   첫 발행 때 SVG 조각 하나를 같이 넣어 워드프레스 통과 여부를 판정한다 —
   통과하면 원형·꺾은선을 추가하고, 막히면 HTML 막대만으로 간다.
3. D-051 이 실제로 되는지 확인. 건강보험 피부양자 원고가 근거 활용 1/13 로 가장
   나쁘다. 「AI 문서 수정」에 "근거에 있는데 본문에 빠진 수치를 채워줘" 만 넣고,
   사람이 값을 찾아주지 않아도 모델이 채우는지 본다.
4. 국민연금 새 원고(content-mtd1j0wv-a7ptt6)는 이미지가 아니라 분량으로 막혀 있다.
   섹션 「신청 전에 확인할 순서와 피해야 할 단정」이 산문 392자 / 필요 400자.
   PublishingGate 세 번째 검사(analyzeLongFormDocument)에서 걸리므로 「품질 다시
   검토」로는 안 풀린다. 8자를 채우거나, 아래 '최소 글자수' 항목을 먼저 정한다.
5. 국민연금 옛 원고(content-mt74z7xg-h6zuwe)와 초안 3778 정리. 발행 수정 두 건
   (이미지 재사용·상태값 검증) 확인은 이 원고로 한다 — 글 3778, 미디어 3777.

== 2026-08-29 조사 결과: 원고 품질 6건 (원인 확정) ==

현금영수증 원고(content-mtehy4hj-nxlmds, 3회 호출 $0.5453)를 실측해서 찾았다.
성격이 다르니 섞지 말 것 — 오류 2건, 빠진 검사 1건, 설계 공백 1건, 의도된 설계 2건.

[오류 1] 생성 스키마가 hero 1장만 허용한다
  app/application/OpenAIProvider.ts 의 structuredGenerationFormat:
    images: { maxItems: 1, items: { additionalProperties: false,
      afterSection: enum[0], purpose: enum["hero"] } }, strict: true
  D-053 에서 프롬프트로 본문 시각물을 요청하게 바꿨는데 스키마를 안 고쳤다.
  모델이 반환할 방법이 구조적으로 없다 — visual·data 는 additionalProperties:false
  로 거부되고, purpose 는 hero 만, 개수는 1개.
  strict 모드는 properties 전부를 required 에 넣어야 하므로, 선택 필드는
  nullable(type:["string","null"])로 만들어야 한다.

[오류 2] 기획의 범위 경계가 본문 문장으로 나간다
  EditorialGenerationStrategy 프롬프트가 `Scope boundaries: ...` 를 그대로 넣는데,
  "이건 작가가 지킬 제약이지 독자에게 할 말이 아니다" 라는 지시가 없다.
  실측: scopeBoundary "개별 결제의 소득공제 반영 결과나 환급액을 계산하지 않는다"
  → 도입부에 "개별 거래의 공제액이나 환급액을 계산하지 않고 …에 집중합니다".

[빠진 검사] 문체를 아무도 안 본다
  프롬프트에 "Write polite, natural Korean" 이 있지만 core/quality 전체에
  문장 종결어미를 보는 검사가 없다. 실측: 문장 70개 중 반말 1개가 그대로 통과.
  ("근로소득자는 … 소득공제를 받을 수 있다.")
  주의: 처음 센 9개는 내 정규식이 "…집니다." 까지 잡은 측정 버그였다.
  존댓말 종결은 니다/세요/십시오 로 판정한다.

[설계 공백] 기획 범위와 근거가 충돌해도 아무도 모른다
  preflight 가 가져온 유일한 수치 자료가 「소득공제금액 계산방법」인데
  기획 범위 경계가 그걸 다루지 말라고 했다. 모델은 기획을 따랐고 수치가 0개가 됐다.
  → 오류 2 와 같은 프롬프트 한 문장으로 함께 해결한다:
    "범위 경계는 근거를 덮어쓰지 않는다. 발췌가 값을 주면 그 값을 쓴다."

[의도된 설계 1] 수치 0개인데 승인 100점
  D-050 에서 concreteness·evidenceUse 를 일부러 weight 0 으로 두었다(비용 때문).
  현금영수증 원고: 본문 2,984자에 수치 0개, 구체성 0점, 근거활용 0/4 → 총점 100 승인.
  차단으로 바꾸는 것은 버그 수정이 아니라 정책 변경이다. 바꾸기 전에
  원고 59편 중 몇 편이 막히는지부터 세고 사장님께 숫자로 보고한다.

[의도된 설계 2] Claim 0개면 출처가 0개가 된다
  ContentPlanningStrategyBase.ts:102 프롬프트: "If no VERIFY or CRITICAL Claim is
  needed, return an empty verificationClaims array; that explicit empty array is a
  valid completed N/A state." 스키마에 minItems 없음, 파서도 빈 배열 통과.
  실측(밝은재테크 32편): Claim 0개 6편 → 출처 0개 6편(예외 0).
                        Claim 1개 이상 26편 → 출처 0개 9편.
  Claim 0개 원고: 휴대폰 요금제 / 비상금 규모 / 생활비 통장 / 관리비 고지서 /
                  생활비 예산 배분 / 월세 예산. 전부 공식 수치가 없는 생활 주제.
  같은 프롬프트가 "빈 배열은 주제가 잘못됐다는 신호이니 다른 주제를 고르라"고
  적어 놨는데 그 판정을 쓰는 코드가 어디에도 없다.

권고한 진행 순서: 오류 2건과 빠진 검사 1건을 먼저 고친다(정책을 안 건드린다).
의도된 설계 2건은 막지 말고 화면에 보이게만 한다 —
「이 주제는 공식 출처가 붙지 않습니다」를 후보 선택 화면에,
「본문에 확인 가능한 수치가 0개입니다」를 발행 화면에.
차단으로 갈지는 그 뒤에 숫자를 세어서 정한다.

미확정으로 남은 것: preflight 가 왜 출처를 1개만 가져왔는지.
approvalSourcePreflightDiagnostic 이 {} 라 확인할 기록이 없다. 추측하지 말 것.

== 원고 품질 ==

- 발행된 13편 중 7편이 본문 수치 0개다. 근거 활용이 낮은 순:
  건강보험 피부양자 1/13, 선택약정 0/4, 본인부담상한액 0/3, 국민연금(옛) 0/3.
- 출처 preflight 가 토큰의 38~62% 를 먹는데 성과와 반대로 간다.
  근로장려금 27,622 토큰 → 수치 26개 / 국민연금(옛) 84,661 토큰 → 수치 1개.
  원고를 몇 편 더 뽑아도 이 비율은 안 바뀐다. 여기를 봐야 한다.
- 네이버식 가독성(한 문장 문단 허용, 장면형 도입부, 3,000~4,500자)은 위 두 개가
  끝난 뒤에 손댄다.

== 코드 ==

- generate 만 restoreProtectedImageAssets 를 안 부른다. revise·improve-quality·
  accept-improvement·final-review 는 전부 부른다. 그래서 원고를 다시 생성하면
  돈 주고 만든 대표이미지가 항상 떨어져 나간다. 고치려면 hero 끼리 짝지어야
  한다 — id 기준이라 그냥 끼우면 이미지 블록이 두 개가 된다.
- 갱신 발행 때 이미지 재업로드 (orphan media 3709/3712). **코드 수정 완료,
  앱 확인 남음.** 발행 기록의 uploadedMedia 로 건너뛰게 했다. 기록에 sourceUrl 이
  없던 옛 건은 readMedia 로 되읽고, 되읽기 실패는 오류가 아니라 재업로드로 간다.
  확인법: 이미 발행한 글을 다시 발행 → 미디어 라이브러리에 새 번호가 안 생기면 됨.
- 공개 Post 갱신 시 draft_status 검증 실패. **코드 수정 완료, 앱 확인 남음.**
  updateDraft 는 status 를 일부러 안 보내는데(공개 글을 초안으로 되돌리지 않으려고)
  검증은 draft 를 기대하도록 굳어 있었다. 갱신 직전에 읽은 그 글의 상태를 기대값으로
  쓴다. 공개 글이 초안으로 떨어지는 것은 여전히 잡는다.
- PublishingGate 의 D-045 잔재 (QualityEngine.ts:149-152). 진단만 됐다.
- 최소 글자수가 결정 로그와 코드가 어긋난다. D-035 는 "최소·선호·최대 글자 수를
  Gate 또는 승인 조건으로 쓰지 않는다"고 하는데, CONTENT_SECTION_PROSE_INSUFFICIENT
  는 400자(목록형 250자)로 발행을 막는다(LongFormDiagnostics.ts:363).
  취지는 "표로 때우지 마라"인데 구현이 숫자라 8자 차이로 막힌다. 셋 중 하나로
  정해야 한다 — 차단에서 빼기 / 글자수가 아닌 기준으로 바꾸기 / 결정 로그에 예외
  명시하기. 고치기 전에 원고 59편 중 몇 편이 걸리는지부터 센다.
- Source Preflight 실패에 진단이 안 남는다. content-mst2r5wl-eadjnz 가
  preflight 에서 막혔는데 approvalSourcePreflightDiagnostic 이 {} 다.
- 프로젝트 전략의 subtopics·excludedTopics 편집 UI 가 없다(읽기 전용).
  지금은 「주제를 직접 입력해 작성」으로 우회한다.

== 화면 ==

- 출처 라벨 "국세청 · 메뉴명없음".
- 표에 "제공된 근거에 수치 미포함" 이 독자에게 그대로 보인다.
- Planning 후보 준비 완료 상태로 멈춘 콘텐츠 4개, Planning 오류 1개.

== 발행 ==

- 발행된 12편 갱신. 나쁜 순: 최신성 확인법, 신용카드 명세서, 대출 상환내역,
  할부 결제 취소(뒤 둘은 제목이 잘려 있다). 삭제가 아니라 갱신으로 간다.
- 색인된 글이 2편뿐이다(site: 실측). 애드센스 최대 리스크.

== 저장소 정리 ==

- 추적되지 않는 파일: .bak-* 2개, approval-diff.txt, test-results/.
- _to_delete/ 폴더는 통째로 지워도 된다. _check/ 도 추적 대상이 아니다.
- .gitattributes 가 없어 CRLF 가 제각각이다. git add -A 를 쓰지 말 것.
  사장님 지시로 병합 후로 미뤘다.
