남은 할 일만 적는다.

작업 규칙·상태 확인법·반복해서 틀린 것은 여기가 아니라
Docs/current/00_FOUNDATION/03_AI_DEVELOPMENT_RULE.md 에 있다.
2026-08-29 이전 기록은 Docs/current/04_DEVELOPMENT/14_WORK_LOG_2026_08.md.

== 2026-09-04 기획 단계 결함 2건 수정 (완료, 커밋 대기) ==

1) expectedCoverage 가 약속한 사실에 Claim 이 없으면 그 항목을 지운다.
   ContentPlanningStrategyBase.ts 에 pruneUnclaimedFactCoverage 추가. 신용카드
   소득공제 원고에서 "공제한도"가 expectedCoverage 에는 있었는데 Claim 은
   0개였던 실측 사례를 근거로 만들었다. 한도·상한·기준액·공제액·지원금·
   이자율·세율 등 숫자를 암시하는 단어가 coverage 에 있는데 어떤 Claim
   에도 그 단어가 없으면 coverage 에서 지운다. 이미 발행된 원고는 안 건드림
   (지난 원고 수정 금지 지시 지킴). 회귀 테스트 3개 추가.
2) 자동 주제 선정이 CRITICAL Claim 없는 주제를 너무 자주 고른다.
   실측: 전체 기획 후보 222개 중 141개(63%)가 CRITICAL Claim 0개 →
   "공식 출처 · 붙지 않음". ContentPlanningStrategyBase.ts 의 자동 선정
   프롬프트(modeInstruction)에 "reader problem 이 자격요건·기한·금액·요율
   중 하나로 검증 가능해야 factual defensibility 로 친다"는 구체적 기준과
   실측 수치를 추가해, 애초에 출처 못 붙는 주제를 덜 고르게 했다. 회귀
   테스트 1개 추가.

검증: tsc·eslint 클린, 전체 유닛 테스트(무관한 기존 결함 3건 제외) 통과.
**원고 1편 실제 기획으로 두 수정이 실제 효과가 있는지는 아직 확인 못 했다**
— 유료 호출(plan, 1회).

== 이어받는 명령 ==

git fetch origin
git switch feat/scheduled-publishing-and-diversity
git pull
npm run dev

== 2026-09-03 AI 비용: 검토 모델 낮춤 + 새로 발견한 낭비 원인 ==

원고 1편 실제 생성(신용카드 소득공제, content-mtljybwx-yweeho)으로 비용을 실측:
$0.5935, preflight 12% / 생성 23% / 품질 검토 64%($0.381). 품질 검토가 비싼
이유는 순수 모델 등급 — gpt-5.6-sol(입력 $5/M·출력 $30/M)이 생성에 쓰는
gpt-5.6-terra(입력 $2.5/M·출력 $15/M)의 2배, preflight의 gpt-5.6-luna 5배.
이 선택에 문서화된 근거가 없어 app/application/OpenAIModelPolicy.ts 의
defaultOpenAIReviewModel 을 gpt-5.6-terra 로 낮췄다(완료). 같은 원고 기준
품질 검토 $0.381→$0.191 추정, 총비용 약 32% 절감. 새 사실 추가를 막는
QualityReviewFactualGuard 는 모델과 무관하게 그대로 작동한다.

**새로 찾은, 아직 안 고친 문제**: 이 검증 과정에서 방금 그 $0.381짜리 검토
호출 자체가 버려졌다는 걸 확인했다 — `qualityReviewAttempt: {outcome:
"discarded", reason: "quality_verified_factual_claim_omitted"}`. 검토 AI가
만든 편집안이 이미 검증된 사실 하나를 빼먹어 안전장치가 결과를 버리고 원래
생성본을 그대로 썼다. 저장된 25편(품질 검토 AI 호출이 있었던 것) 중 이
필드가 남은 3편이 전부 같은 사유로 버려졌다 — 나머지 22편은 이 필드가 없어서
성공했는지 확인 불가(성공 시엔 이 필드를 안 남기는 것으로 보임, 추측임).

이건 2026-08-19 커밋(8685dd4 "채택될 수 없는 편집 호출을 하지 않고, 그 결말을
기록한다")이 고친 문제와 증상은 같지만(둘 다 "검토 호출이 버려진다") 원인은
다르다. 8/19 건은 생성본이 이미 규칙 채점 100점이면 검토본이 구조적으로 절대
못 이겨서(betterThan 조건) 매번 버려지는 문제였고, 그건 "규칙 채점이 이미
정식 승인+차단 0건이면 AI를 아예 안 부른다"는 스킵 로직으로 고쳐졌다(지금도
정상 작동 — 오늘 검토가 실제로 불렸다는 것 자체가 그때는 스킵 조건에 안
걸렸다는 뜻). 오늘 발견한 건 QualityReviewFactualGuard(2026-08-09 도입, 8/19
수정 범위 밖)가 검증된 사실 누락을 잡아 버리는 별개의 경로다.

**원인 확정, 수정 완료**: 검토 프롬프트(EditorialQualityPipeline.ts 의
singlePassFinalReviewInstruction)는 "claim 인벤토리를 필드 하나까지 원본과
똑같이 돌려달라"고 요구하는데, 정작 그 원본 데이터(document.metadata.
generatedFactualClaimInventory)를 모델에게 한 번도 보여주지 않았다 —
contentDocumentAIContext(35행)가 이 필드를 제외 목록에 넣어 프롬프트에서
걸러냈기 때문이다. surfaceText(본문 문장)는 눈에 보이니 재구성할 수 있어도
normalizedValueJson·qualifiers·evidenceUrl·evidenceExcerpt는 독자에게 안
보이는 백엔드 전용 값이라 모델이 알 방법이 구조적으로 없었다. 오늘 앞서 고친
[오류 1](스키마가 프롬프트 요구를 반환 못 하게 막음)과 같은 종류의 결함이다.
D-051이 고친 것과 비슷해 보이지만 그건 approvalEvidence(발췌) 필드였고
revise 호출만 고쳤다 — generatedFactualClaimInventory 는 손대지 않은 채였다.

고침: singlePassFinalReviewInstruction 의 claim 인벤토리 지시문 옆에
document.metadata.generatedFactualClaimInventory 를 그대로 직렬화해 넣었다.
회귀 테스트 추가(EditorialQualityPipeline.test.ts, 지시문에 claimId·
evidenceUrl 이 실제로 실리는지 확인). tsc·eslint·전체 유닛 테스트(무관한
기존 결함 3건 제외) 통과. **원고 1편 재생성으로 검토가 실제로 채택되는지는
아직 확인 못 했다** — 유료 호출.

- 아직 남은 것: 검토 모델을 sol→terra 로 낮춘 것이 이 "정교한 지시 준수"
  준수율을 떨어뜨리는지 아직 모른다. 이제 프롬프트에 원본 데이터가 실리니
  준수 자체는 훨씬 쉬워졌지만, terra 가 sol 만큼 잘 지키는지는 원고 몇 편을
  더 생성해서 discard 비율로 실측해야 한다.

== 지금 바로 ==

1. D-053 확인. 본문 시각물(막대·비율·단계·타임라인·비교·수치 타일)이 새 원고
   생성에서 실제로 나오는지 본다. 원고 1편 생성이 완료 기준이다.
   생김새 미리보기: _check/bright-visual-preview.html (렌더러 실제 출력).
   첫 발행 때 SVG 조각 하나를 같이 넣어 워드프레스 통과 여부를 판정한다 —
   통과하면 원형·꺾은선을 추가하고, 막히면 HTML 막대만으로 간다.
   2026-09-03: 이 확인이 지금까지 항상 실패할 수밖에 없었던 원인(아래 [오류 1]
   완료 항목)을 고쳤다. 이전에는 구조상 모델이 반환할 방법이 없었으니, 이제
   처음으로 의미 있는 확인이 된다.
2. D-051 이 실제로 되는지 확인. 건강보험 피부양자 원고가 근거 활용 1/13 로 가장
   나쁘다. 「AI 문서 수정」에 "근거에 있는데 본문에 빠진 수치를 채워줘" 만 넣고,
   사람이 값을 찾아주지 않아도 모델이 채우는지 본다.
3. 국민연금 새 원고(content-mtd1j0wv-a7ptt6)는 이미지가 아니라 분량으로 막혀 있다.
   섹션 「신청 전에 확인할 순서와 피해야 할 단정」이 산문 392자 / 필요 400자.
   PublishingGate 세 번째 검사(analyzeLongFormDocument)에서 걸리므로 「품질 다시
   검토」로는 안 풀린다. 8자를 채우거나, 아래 '최소 글자수' 항목을 먼저 정한다.
4. 국민연금 옛 원고(content-mt74z7xg-h6zuwe)와 초안 3778 정리. 발행 수정 두 건
   (이미지 재사용·상태값 검증) 확인은 이 원고로 한다 — 글 3778, 미디어 3777.
   이 원고가 그 확인의 첫 실제 기회다 — 근로장려금 쪽은 문서 리비전이 안 바뀌어
   idempotency 로 기존 기록을 그대로 돌려받았을 뿐, 이미지 재사용 코드 경로
   (7장 mediaPlan → reusableMediaUrl)를 실제로 타지는 않았다(아래 완료 기록 참고).

== 2026-09-03 D-052 확인 결과 (완료) ==

근로장려금·국민연금 두 원고 편집기에서 직접 확인. 내부 링크·관련 글이 전부
다른 글만 가리키고 자기 자신은 없다(승인 준비 「내부 링크」 검사도 둘 다 통과).

근로장려금을 「워드프레스 임시저장」으로 실제로 눌러 발행했다. 응답:
verification.verified: true, 9개 검사 전부 passed, externalId "3784"
(기준값과 동일), 미디어 "3785"/asset db083ffa 그대로. .bright-studio/
studio-data.json 을 직접 대조해 publishingRecords 가 여전히 2건, 둘 다
타임스탬프가 8/28 그대로임을 확인했다 — 새 기록이 생기지 않았다.

단 응답에 reused: true 가 찍혔다. 문서 리비전(rev-fcdcb0aa)이 8/28 마지막
발행과 같아서 「2. 중복 기록을 먼저 본다」 단계에서 기존 검증 결과를 그대로
돌려준 것이다. 검증 통과·미디어 중복 없음 자체는 실측했지만, 이미지 재사용
스킵 로직(7장)을 새로 태운 건 아니다. 그 코드 경로를 실제로 확인하려면
문서를 한 글자라도 고쳐 리비전을 바꾼 뒤 다시 발행해야 한다(위 '지금 바로' 4번).

== 2026-08-29 조사 결과: 원고 품질 6건 (원인 확정) ==

현금영수증 원고(content-mtehy4hj-nxlmds, 3회 호출 $0.5453)를 실측해서 찾았다.
성격이 다르니 섞지 말 것 — 오류 2건, 빠진 검사 1건, 설계 공백 1건, 의도된 설계 2건.
2026-09-03: 오류 2건 · 빠진 검사 1건 · 설계 공백 1건 코드 수정 완료, 타입체크·
린트·전체 유닛 테스트(무관한 기존 결함 3건 제외) 통과. **원고 1편 실제 생성으로
본문 시각물이 나오는지는 아직 안 봤다** — 유료 호출이라 사장님이 눌러야 한다
(위 '지금 바로' 2번).

[오류 1] 생성 스키마가 hero 1장만 허용한다 — 완료
  app/application/OpenAIProvider.ts 의 structuredGenerationFormat 을 고쳤다.
  images: maxItems 1→4, purpose enum ["hero"]→hero 포함 6종, afterSection 을
  0 고정에서 0 이상 정수로, visual·data 프로퍼티를 추가했다(선택 필드는
  strict 모드 때문에 required 에 넣고 "" · [] · null 로 표현). 프롬프트
  (EditorialGenerationStrategy.ts)에도 이 여섯 필드를 항상 채우라는 문장과
  예시 JSON을 맞춰 넣었다. 파서(structuredBlocks·parseBlock·normalizeVisualShape·
  normalizeVisualData)는 이미 이 모양을 기대하고 있었으므로 안 고쳤다.

[오류 2 + 설계 공백] 범위 경계가 본문으로 새고 근거를 덮어쓴다 — 완료
  EditorialGenerationStrategy.ts 의 `Scope boundaries: ...` 앞에 "이건 작가
  제약이지 독자에게 할 말이 아니다, 그리고 근거가 준 값은 범위 경계보다
  우선한다" 는 문장 하나를 추가했다(같은 원인이라 한 문장으로 함께 닫힘).

[빠진 검사] 문체를 아무도 안 본다 — 완료
  core/quality/SentenceFormality.ts 를 새로 만들어 문장 종결이 니다/세요/십시오
  가 아니면서 평서형(-다)으로 끝나는 문장을 센다. concreteness·evidenceUse 와
  같은 표시 전용(weight 0) dimension "formality" 로 QualityEngine 에 붙였다 —
  발행·승인 조건은 그대로다. 화면 라벨("문체(존댓말)")도 3곳 맞춰 넣었다.
  실제 원고에서 몇 건이나 잡히는지는 아직 안 셌다 — 원고 1편 생성 확인(위 2번)
  때 같이 본다.

[의도된 설계 1] 수치 0개인데 승인 100점
  D-050 에서 concreteness·evidenceUse 를 일부러 weight 0 으로 두었다(비용 때문).
  현금영수증 원고: 본문 2,984자에 수치 0개, 구체성 0점, 근거활용 0/4 → 총점 100 승인.
  차단으로 바꾸는 것은 버그 수정이 아니라 정책 변경이다. 바꾸기 전에
  원고 59편 중 몇 편이 막히는지부터 세고 사장님께 숫자로 보고한다.

[의도된 설계 2] Claim 0개면 출처가 0개가 된다
  ContentPlanningStrategyBase.ts:102 프롬프트: "If no VERIFY or CRITICAL Claim is
  needed, return an empty verificationClaims array; that explicit empty array is a
  valid completed N/A state." 스키마에 minItems 없음, 파서도 빈 배열 통과.
  실측(밝은재테크 32편): Claim 0개 6편 → 출처 0개 6편(예외 0).
                        Claim 1개 이상 26편 → 출처 0개 9편.
  Claim 0개 원고: 휴대폰 요금제 / 비상금 규모 / 생활비 통장 / 관리비 고지서 /
                  생활비 예산 배분 / 월세 예산. 전부 공식 수치가 없는 생활 주제.
  같은 프롬프트가 "빈 배열은 주제가 잘못됐다는 신호이니 다른 주제를 고르라"고
  적어 놨는데 그 판정을 쓰는 코드가 어디에도 없다.

의도된 설계 2건은 아직 그대로다 — 막지 말고 화면에 보이게만 한다 —
「이 주제는 공식 출처가 붙지 않습니다」를 후보 선택 화면에,
「본문에 확인 가능한 수치가 0개입니다」를 발행 화면에.
차단으로 갈지는 그 뒤에 숫자를 세어서 정한다.

미확정으로 남은 것: preflight 가 왜 출처를 1개만 가져왔는지.
approvalSourcePreflightDiagnostic 이 {} 라 확인할 기록이 없다. 추측하지 말 것.

== 원고 품질 ==

- 발행된 13편 중 7편이 본문 수치 0개다. 근거 활용이 낮은 순:
  건강보험 피부양자 1/13, 선택약정 0/4, 본인부담상한액 0/3, 국민연금(옛) 0/3.
- 출처 preflight 가 토큰의 38~62% 를 먹는데 성과와 반대로 간다.
  근로장려금 27,622 토큰 → 수치 26개 / 국민연금(옛) 84,661 토큰 → 수치 1개.
  원고를 몇 편 더 뽑아도 이 비율은 안 바뀐다. 여기를 봐야 한다.
- 네이버식 가독성(한 문장 문단 허용, 장면형 도입부, 3,000~4,500자)은 위 두 개가
  끝난 뒤에 손댄다.

== 코드 ==

- generate 만 restoreProtectedImageAssets 를 안 부른다. revise·improve-quality·
  accept-improvement·final-review 는 전부 부른다. 그래서 원고를 다시 생성하면
  돈 주고 만든 대표이미지가 항상 떨어져 나간다. 고치려면 hero 끼리 짝지어야
  한다 — id 기준이라 그냥 끼우면 이미지 블록이 두 개가 된다.
- 갱신 발행 때 이미지 재업로드 (orphan media 3709/3712). **코드 수정 완료,
  앱 확인 남음.** 발행 기록의 uploadedMedia 로 건너뛰게 했다. 기록에 sourceUrl 이
  없던 옛 건은 readMedia 로 되읽고, 되읽기 실패는 오류가 아니라 재업로드로 간다.
  확인법: 이미 발행한 글을 다시 발행 → 미디어 라이브러리에 새 번호가 안 생기면 됨.
- 공개 Post 갱신 시 draft_status 검증 실패. **코드 수정 완료, 앱 확인 남음.**
  updateDraft 는 status 를 일부러 안 보내는데(공개 글을 초안으로 되돌리지 않으려고)
  검증은 draft 를 기대하도록 굳어 있었다. 갱신 직전에 읽은 그 글의 상태를 기대값으로
  쓴다. 공개 글이 초안으로 떨어지는 것은 여전히 잡는다.
- PublishingGate 의 D-045 잔재 (QualityEngine.ts:149-152). 진단만 됐다.
- 최소 글자수가 결정 로그와 코드가 어긋난다. D-035 는 "최소·선호·최대 글자 수를
  Gate 또는 승인 조건으로 쓰지 않는다"고 하는데, CONTENT_SECTION_PROSE_INSUFFICIENT
  는 400자(목록형 250자)로 발행을 막는다(LongFormDiagnostics.ts:363).
  취지는 "표로 때우지 마라"인데 구현이 숫자라 8자 차이로 막힌다. 셋 중 하나로
  정해야 한다 — 차단에서 빼기 / 글자수가 아닌 기준으로 바꾸기 / 결정 로그에 예외
  명시하기. 고치기 전에 원고 59편 중 몇 편이 걸리는지부터 센다.
- Source Preflight 실패에 진단이 안 남는다. content-mst2r5wl-eadjnz 가
  preflight 에서 막혔는데 approvalSourcePreflightDiagnostic 이 {} 다.
- 프로젝트 전략의 subtopics·excludedTopics 편집 UI 가 없다(읽기 전용).
  지금은 「주제를 직접 입력해 작성」으로 우회한다.
- 2026-09-03 전체 유닛 테스트 중 이번 작업과 무관하게 이미 깨져 있던 것 3건을
  찾았다(git stash 로 대조해 이번 세션 변경 전에도 실패함을 확인). 손대지 않고
  기록만 남긴다.
  1) tests/unit/core/Epic1.test.ts:102 가 `report.dimensions`를 10개로 기대하는데
     실제로는 그때도 13개(지금은 14개)였다. concreteness 등 표시 전용 dimension을
     추가하면서 이 자리만 안 고친 낡은 값으로 보인다 — 확인 안 함, 추측하지 말 것.
  2) tests/unit/app/api/QualityImprovementCandidateRoute.test.ts 의
     "places the confirmed keyword..." 가 실패한다. 입력 문서의 이미지 블록에
     purpose 가 없으면(비어 있으면) EditorialGenerationStrategy.ts 의
     ensureEditorialPlacement 가 `block.purpose === "hero"` 검사에 걸려 이미
     대표 이미지가 있는데도 "hero-image" 를 하나 더 끼워 넣어 이미지가 2장이
     된다. quality-final-edit/quality-auto-improvement 경로(editorialDocumentFormat,
     blocks 그대로 파싱)에서 재현된다.
  3) tests/unit/app/application/IntegrationServices.test.ts 의 "removes
     source-empty body image recommendations during canonical parsing" 가
     이미지 1장(대표만 남아야 함)을 기대하는데 2장이 나온다. 위 2)와 같은
     ensureEditorialPlacement 코드 경로로 보이지만 원인을 끝까지 추적하지
     않았다 — 같은 결함인지 별개인지 확인 필요.

== 화면 ==

- 출처 라벨 "국세청 · 메뉴명없음".
- 표에 "제공된 근거에 수치 미포함" 이 독자에게 그대로 보인다.
- Planning 후보 준비 완료 상태로 멈춘 콘텐츠 4개, Planning 오류 1개.

== 발행 ==

- 발행된 12편 갱신. 나쁜 순: 최신성 확인법, 신용카드 명세서, 대출 상환내역,
  할부 결제 취소(뒤 둘은 제목이 잘려 있다). 삭제가 아니라 갱신으로 간다.
- 색인된 글이 2편뿐이다(site: 실측). 애드센스 최대 리스크.

== 저장소 정리 ==

- 추적되지 않는 파일: .bak-* 2개, approval-diff.txt, test-results/.
- _to_delete/ 폴더는 통째로 지워도 된다. _check/ 도 추적 대상이 아니다.
- .gitattributes 가 없어 CRLF 가 제각각이다. git add -A 를 쓰지 말 것.
  사장님 지시로 병합 후로 미뤘다.
