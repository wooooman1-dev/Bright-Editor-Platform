== 이어받는 명령 ==
git fetch origin
git switch feat/scheduled-publishing-and-diversity
git pull
npm run dev

npm install 은 이전 브랜치에서 뒤처져 있을 때만. 이번 작업은 package.json 을
건드리지 않았다.

주의: 이 세션에서 커밋한 3개(22f7329, 2c67dd1, d972cc3)가 세션 종료 시점에
origin 에 아직 push 안 됐다(`git status -sb` 로 확인: "ahead 3", origin HEAD 는
fe07307 에 머물러 있음). 다음 세션 시작 시 반드시
`git log origin/feat/scheduled-publishing-and-diversity -3` 로 push 여부부터
확인할 것 — push 가 안 됐다면 사장님 지시 없이 임의로 push 하지 말고(위
CLAUDE.md/AGENTS.md 원칙상 커밋·푸시는 사용자가 지시할 때만) 먼저 확인부터
받는다.

== 현재 상태 (2026-08-14, d972cc3) ==
브랜치 feat/scheduled-publishing-and-diversity, HEAD = d972cc3, origin 보다
3커밋 앞섬(push 안 됨). `git status --short` 클린 — 미커밋 변경 없음.

이번 세션은 fe07307(지난 세션 handover) 이후 순수 버그 수정 3개 커밋을
추가했다. "feat/scheduled-publishing-and-diversity" 브랜치 이름이 가리키는
원래 기능(예약 발행, 다양성) 자체의 진행 상황은 이번 세션에서 손대지 않았다
— 그 부분은 이 파일의 과거 기록(아래 남겨둔 절 밖의 것은 이미 지워졌으니
git log 로 확인)을 참고할 것.

각 커밋 전 `npx tsc --noEmit` 클린 확인. 최종적으로
`npx vitest run tests/unit` → 323 files / 1991 tests 전부 통과(3개 커밋
합산 최종 실행), `npx eslint .` 클린 확인 — 전부 이번 세션에 실제로 실행한
결과다.

1. `22f7329 fix: write unverified example numbers as prose, not compressed
   numerals` — approval-evidence 담당. `core/ai/ApprovalSourcePreflight.ts`의
   `withApprovalSourcePreflightInstruction` 두 갈래(canonical Claim-ID 번들 /
   ad-hoc 출처 번들) 모두에 "검증된 Claim이 아닌 예시·주기·근사 숫자는
   압축형(1주) 대신 서술형으로 써라" 지시를 추가했다 — 제목·헤딩·리스트
   라벨·표 셀 포함. 순수 프롬프트 예방책, 게이트(`GeneratedClaimBinding.ts`)
   는 무변경. 신규 테스트
   `tests/unit/core/ai/ApprovalSourcePreflightInstruction.test.ts`.
   지난 세션이 "1주 수면 습관 점검표" 오탐을 "문법 신호가 전혀 없어 게이트로
   못 고친다"고 남긴 항목(다음 할 일 2번)을 이 프롬프트 수정으로 처리한
   것 — 재발 여부는 실제 생성으로만 확인 가능(아래 "다음 할 일" 참고).

2. `2c67dd1 fix: keep withdrawn-Claim removal from gutting section prose`
   — quality-root-cause 담당. **이번 세션 핵심 발견**: 사장님이 "차단이
   며칠째 안 풀린다"고 지적한 `CONTENT_SECTION_PROSE_INSUFFICIENT`의 진짜
   원인을 실측(`content-msrfq4gt-fc8ub1` 재현)으로 확정했다. 게이트도
   400/250자 하한도 문제가 아니었다 — 생성 직후엔 원고가 하한을 여유 있게
   통과한다(실측: 411/497자 vs 250/400 하한). 문제는 생성 *이후*
   `core/approval/GeneratedFactualClaimInventory.ts`의
   `findUntrackedCriticalSurfaces` → `removeGeneratedFactualSurface`가
   "인벤토리에 미보고된 critical 패턴 문장"을 발견하면 그 문장이 든
   **문단 전체**를 삭제하는 것이었다 — 삭제된 문단 중엔 표를 설명하던
   문단, 정보 기준일 날짜(정책상 오히려 필수)가 섞여 있었다. 화면의
   "산문 부족" 진단 문구가 실제 원인(문단 삭제)을 전혀 가리키지 않고
   있었다.
   - `core/ai/AIWorkflow.ts`: `factualInventoryGeneration`(승인 모드) 전용
     "Withdrawal-resilient paragraph contract" 추가 — 검증 대상 사실은
     독립 문단에, 표/리스트 설명 산문과 섞지 말고, 하한은 비-사실 문단만
     으로 채우라고 지시. 수익형 콘텐츠 경로는 안 건드림(위 pinned memory
     "adsense-approval-vs-revenue-content-scope" 준수).
   - `app/application/EditorialQualityPipeline.ts`: `manuscriptDiagnostics`
     가 `CONTENT_INCOMPLETE_SECTION`만 2차(복구) 호출에 전달하고 정작
     차단 사유인 `CONTENT_SECTION_PROSE_INSUFFICIENT`는 전달 안 하던
     버그 수정. 이제 `blockingViolations`/`sectionProseShortfalls`
     (heading·minimum·actual)를 전달하고 "새 사실 없이 설명 산문만으로
     채워라" 지시 추가.
   - `core/content/LongFormDiagnostics.ts`: 판정 로직 **무변경**.
     `core/content/index.ts`가 400/250 상수를 `longFormNarrativeFloors`로
     export해서 프롬프트 3곳 하드코딩 숫자가 게이트와 어긋나지 않게 함.
   - 신규 테스트: `tests/unit/core/ai/
     AIWorkflowWithdrawableFactIsolation.test.ts`.
   - **미해결(삭제 로직 자체는 이번에 손 안 댐, 아래 "다음 할 일" 1번
     참고)**: (1) 삭제 단위가 문장이 아니라 문단 전체, (2)
     `criticalSurfacePattern`이 정책상 필수인 정보 기준일 날짜까지
     critical로 오분류, (3) 생성이 Claim 인벤토리 보고를 누락하는 계약
     위반 자체가 방치됨(한 원고에서 `unreported_generated_critical` 4건
     발생), (4) 삭제 후 `longFormStructure`가 삭제된 블록 ID를 그대로
     참조(desync). 이 4가지가 해결돼야 "차단 완전 해제"와 (아래 3번의)
     "출처 소실"이 근본적으로 풀린다.

3. `d972cc3 fix: stop hiding the Evidence card and unreachable readiness
   aggregates` — approval-readiness-designer 담당. 사장님이 "출처가
   표시되지도 않고 출처 검토도 안 된다"고 지적한 원인 3가지를 밝은재테크
   실 원고 19편 실측으로 확정:
   - `app/user-flow/quality-review-ui.ts`의
     `visibleApprovalReadinessChecks`가 `applicable !== false`만 통과시켜서,
     mandatory Claim이 없어 `applicable: false`로 판정되는 Evidence 체크가
     **패널에서 통째로 사라지고** 있었다(19편 중 8편이 이 경로, 그중 4편은
     실제로 출처를 못 찾아 문장이 빠진 인벤토리 보유 — 시스템이 한 일이
     화면에서 안 보였다). 이제 "필수 검증 대상 아님" 라벨로 표시.
   - `core/quality/QualityEnginePolicy.ts`의 `standardQualityCheck`가
     "품질 미승인"이라고만 말하고 이유를 안 줌 — 차단된 8편 중 5편이
     점수는 전부 100점이라 점수판도 카드도 진짜 차단 사유를 안 알려주고
     있었다. `standardQualityBlockingReasons` 신설, 차단된 task 메시지를
     모아 카드에 표시.
   - `app/application/approval/ApprovalAwarePersistenceStore.ts`의
     `reconcileApprovalReadinessAggregates`가 기존 aggregate가 없으면 새로
     derive 자체를 안 했음 — Quality report와 정책 스냅샷은 있는데
     aggregate가 한 번도 없었던 원고 3편(19편 중)은 승인 준비 패널이
     통째로 안 떴다. 이제 최초 1회도 derive·저장하도록 수정.
   - 관련 파일: `core/approval/ApprovalReadiness.ts`,
     `core/approval/ApprovalReadinessDerivation.ts`,
     `app/application/approval/ApprovalReadinessApplicationServiceBase.ts`,
     `app/user-flow/EditorWorkspaceImplementation.tsx`.
   - 이 커밋을 만들던 에이전트가 세션 API 사용량 한도로 중간에 죽었지만
     (재개 불필요, 실측·수정·테스트 모두 이미 완료된 상태였음) 커밋 직전
     검증 중 스크래치 파일
     `tests/unit/core/approval/ZzLiveReadinessProbe.test.ts`(세션 임시
     디렉터리의 `live.json`을 절대경로로 읽는 디버깅용 코드, 실제
     assertion 없음)를 발견해서 커밋에서 제외하고 삭제했다 — 다음 세션은
     이 파일이 없다는 걸 이상하게 여기지 않아도 된다.

세션 중 확인한 사실(오해 정정용): "3단계 AI 호출(출처 프리플라이트+생성+
품질검토)"은 `contentPurpose: adsense_approval`일 때만 적용되는 의도된
예외라는 걸 `core/approval/ApprovalPolicy.ts:255-272`,
`core/ai/AIWorkflow.ts:105-116` 코드로 확인했다 — 수익형 콘텐츠는 여전히
Decision Log 원칙대로 2콜이다.

== 다음 할 일 ==
0. (선행) `git log origin/feat/scheduled-publishing-and-diversity -3` 로
   이번 세션 3개 커밋(22f7329, 2c67dd1, d972cc3)이 push 됐는지 확인한다.
   이 handover 작성 시점엔 미확정(ahead 3) — 직접 확인하고 push 여부가
   불확실하면 사장님께 먼저 확인받는다.
1. `core/approval/GeneratedFactualClaimInventory.ts`의 문단 단위 삭제를
   approval-evidence에게 맡겨 고친다 — 위 2번 커밋의 "미해결" 4항목
   (문장 단위 삭제, 정보 기준일 오분류, 인벤토리 미보고 계약, structure
   desync). 이게 풀려야 "차단"과 "출처 소실" 둘 다 근본적으로 해소된다.
2. 실제 원고 생성으로 재검증한다 — 이번 세션은 단위 테스트로만
   재현·검증했고(OpenAI 호출 비용 절약), 사장님이 직접 브라우저에서
   생성해보겠다고 했다. 그 결과를 다음 세션이 확인해야 한다 — 특히 위
   1번 커밋(예시 숫자 서술형) 재발 여부와 위 2번 커밋(문단 삭제) 이후
   실제 차단 해제 여부.
3. `content-mscjq0sq-xd8kp5`(정기결제 해지 방법, `genericClaim:cdusyk`
   출처 미확보) 문제는 지난 세션이 "다음 할 일 1번"으로 남겼던 항목인데,
   이번 세션에 확인해보니 이 기계의 `.bright-studio/studio-data.json`에는
   애초에 이 콘텐츠가 없다(사장님이 "워크스페이스가 다르다"고 확인). 이
   콘텐츠가 있는 다른 기계에서 재확인할 것.
4. identity 제외 목록(`canonicalizerOwnedFields`) 상한을 고정하는 테스트를
   `tests/unit/app/application/approval/
   ApprovalReadinessAggregateIntegrity.test.ts` 에 추가한다(지난 세션
   13번 후속 과제, 이번 세션엔 손 안 댐) — URL 재정규화나 provenance
   변경처럼 "여전히 재검증돼야 하는" 케이스를 다음에 제외 목록을 넓힐 때
   아무 테스트도 안 울리는 문제. 다음에 승인 관련 작업을 할 때 같이
   처리해도 된다.

== 보류/관찰 중인 항목 ==

- 줄바꿈(CRLF) 정리: 이전 세션 코드 diff 일부가 파일 전체의 줄바꿈을
  LF→CRLF로 바꿔 diff를 부풀렸다 (예: app/application/OpenAIProvider.ts).
  저장소에 .gitattributes 가 없어 파일마다 줄바꿈이 제각각이다. 사장님이
  "병합 후로 미뤄"라고 지시했다 — 이 브랜치가 main 에 병합된 뒤 별도
  `chore: normalize line endings` 커밋으로 처리한다. 지금 하면 진행 중인
  다른 수정과 diff가 뒤섞인다.

== 따라가지 않는 것 ==
.bright-studio/ 는 git 대상이 아니다. 이번 세션이 실측한 밝은재테크 원고
19편, 진단 스크립트 산출물은 이 기계에만 남고 옮겨지지 않는다. 다음
기계에 밝은재테크 Project 와 기존 발행 글이 있어야 반복 회피 컨텍스트와
내부 링크 카탈로그, 승인 준비 게이트 검증이 같은 조건으로 동작한다.
