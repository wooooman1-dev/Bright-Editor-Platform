== 이어받는 명령 ==
git fetch origin
git switch feat/scheduled-publishing-and-diversity
git pull
npm run dev

npm install 은 이전 브랜치에서 뒤처져 있을 때만. 이번 작업은 package.json 을
건드리지 않았다.

== 현재 상태 (2026-08-13, 288c29d) ==
브랜치 feat/scheduled-publishing-and-diversity, origin 과 동기화(push 완료).
중간에 ec17c6b 하나를 커밋 메시지 제목 중복(0f97d1a 와 동일 제목이었음)
때문에 46dc38e 로 reword 했다 — 그 뒤 커밋 3개가 이 리라이트로 해시가
바뀌어 force-with-lease 로 다시 push 했다. 내용은 리베이스 전후로 트리
diff 없음(확인됨), 코드 변경 아님.
격리 재검증: tsc --noEmit 클린, eslint 클린, vitest 전체 644 files / 3881
tests passed, 36 skipped, 2 failed — 실패 2건은
tests/unit/app/application/ConnectionInfrastructure.test.ts (Windows DPAPI
SecretStore, .claude/worktrees/ 사본까지 중복 실행되는 것 포함) 이고
approval/Claim 작업과 무관. 단독 실행하면 10/10 통과 — 전체 스위트 동시
실행 시에만 흔들리는 환경성 실패로 판단.

부수 발견: vitest 가 .claude/worktrees/ 안의 코드까지 테스트로 스캔하고
있다(위 실패가 같은 테스트를 두 경로에서 두 번 돌린 것으로 나타남).
vitest.config.ts 에 worktrees 제외 설정이 없다 — 다음 세션에서 손볼 것.

e65e57f 이후 이번 세션에서 커밋이 여러 개 더 올라갔다 (git log 로 확인).

b509cff 에서 생성 원고 한 편을 측정해 근본 원인 3건을 고쳤다. 자세한 내용은
Docs/current/04_DEVELOPMENT/13_EDITORIAL_DIVERSITY_HANDOFF.md 12절.

1. QualityEngine 의 planningPattern 에 있던 맨 `작성할` 이 독자용 체크리스트
   제목을 오탐해 완결성·유용성에서 45점을 깎고 있었다. 이제 원고를 목적어로
   잡을 때만 걸린다.
2. ContentPlanningStrategyBase 의 kind 하한이 모델이 risk 를 생략했을 때만
   적용돼, eligibility 클레임에 verify 라고 답하면 출처 사전검증 자체를
   건너뛰었다. 워크스페이스 9건 중 8건이 이 경로로 출처 없이 발행됐다.
   이제 kind 하한이 우선이다.
3. 산문 하한(400자, 리스트형 250자)과 비교 이행 의무를 생성 프롬프트에
   명시했다. 게이트는 있었지만 생성에게 알려주지 않고 있었다.

이번 세션에서 위 수정 이후 실제로 원고 생성을 두 번 시도했고 둘 다 다른
지점에서 막혔다. 둘 다 해당 원고를 손보지 않고 근본 원인을 코드로 고쳤다.

4. 1차 실패: source_preflight 단계가 `OpenAI response was incomplete:
   max_output_tokens` 로 끊겼다. app/application/OpenAIProvider.ts 의
   editorialOutputPolicy 에서 approval-source-preflight 태스크만
   maxOutputTokens: 4_000 이었고, 추론 토큰이 그 예산 안에서 소비돼 추론이
   3,628 을 쓰고 372 만 남아 구조화 출력을 못 냈다. 다른 추론 단계(기획
   12,000 / 생성 11,000~14,000 / 품질 12,000)와 같은 12,000 으로 올렸다.
5. 2차 실패: 예산을 올린 뒤 새로 돌린 생성이 `coverage_incomplete` 로
   막혔다 — 필수 Claim 2건 중 1건("피부양자 자격 요건")이 끝내 출처를 못
   찾았다. 실제로는 국민건강보험공단 자기 사이트
   (nhis.or.kr/nhis/policy/wbhada07400m01.do) 에 그 요건이 평문 11,250자로
   있는데, 탐색이 두 번의 시도 모두 law.go.kr 조문정보 안에서만 맴돌았다.
   원인은 core/ai/ApprovalSourcePreflight.ts 의 재시도 피드백이 "거부된
   URL"만 담고 "출처가 하나도 없는 미확보 Claim"은 담지 않아, 두 번째
   시도가 같은 실패한 검색을 반복한 것. ApprovalSourceClaimGapNote 를
   추가해 미확보 Claim을 claimId+field 로 재시도 프롬프트에 실었고, 거부
   URL이 없어도(순수 커버리지 공백만 있어도) 재시도가 일어나게 했다.
   판정 기준(공식성/관련성/앵커/의미검증)은 전혀 완화하지 않았다 — 후보를
   하나 더 얻게 한 것뿐이다.
6. 4·5번 두 수정은 모두 같은 커밋 0f97d1a 에 들어 있다 (한 회차 재현
   조사·수정으로 같이 처리됨). 회귀 테스트: tests/unit/app/application/
   OpenAIApprovalWebSearch.test.ts (예산 값 갱신), tests/unit/core/ai/
   ApprovalSourcePreflightRetry.test.ts (커버리지 공백 재시도 테스트 2건
   추가). 둘 다 tsc --noEmit 클린, vitest 전체 통과 확인됨.
7. 세 번째 생성 시도(건강보험 피부양자 원고)가 실제로 끝까지 갔다. 완결성·
   유용성 100, 출처 2건 critical_verified, 표·리스트 블록으로 비교/체크리스트
   요구도 충족 — 1·2·3번 측정 통과. 다만 quality.approved 는 false 였다 —
   점수와 승인 게이트는 별개 상태라는 걸 이번에 직접 확인했다 (AGENTS.md
   14장). 아래 8·9번이 그 원인.
8. GeneratedFactualClaim.ts 의 verbatim anchor 불일치(위에서 "관찰 대기"로
   남겨뒀던 항목) — 원인이 확정돼 커밋 46dc38e 로 고쳤다. 실제 원인은
   재작성 단계의 텍스트 손상이 아니라, GeneratedVerifyEvidence.ts 가 Claim의
   위험도를 저장된 verificationPlan 이 아니라 Generation 자신이 붙인 라벨로
   판정하던 것 — 서버가 이미 critical 로 사전검증한 Claim이 모델이 스스로
   "verify" 라고 적으면 best-effort 경로로 떨어져, 그 경로가 요구하는
   "같은 Generation 호출의 인용"이 없어서 탈락했다. 관련해서
   generatedFactualClaimInventory 와 generatedClaimVerification 스냅샷이
   같은 리비전 안에서 서로 다른 답을 갖던 것도 같이 고쳤다. 회귀 테스트
   tests/unit/core/approval/GeneratedFactualClaim.test.ts,
   GeneratedClaimVerificationIntegrity.test.ts, tests/unit/core/ai/
   GeneratedVerifyEvidence.test.ts. 실제로 막혔던 문서를 재생해
   evaluateGeneratedClaimVerificationIntegrity 가 passed: true 로 바뀌는
   것까지 확인함.
9. "1년/2년" 미검증 고위험 수치 오탐(GeneratedClaimBinding.ts) 수정을
   approval-quality-auditor 가 시도했으나 fix-verifier 독립 검증에서
   **실패** 판정을 받았다 — 커밋하지 않고 git stash 에 보관 중
   ("approval-quality-auditor: FAILED per fix-verifier, needs rework").
   git stash list 로 확인. 완화 폭이 의도보다 넓어서, "최근 6개월 이내에
   폐업한 사업자만 신청" 같은 진짜 외부 사실 주장까지 조용히 면제된다
   (독자 기록 지시 문맥 판정이 고정 어휘 사전에만 의존). 또한 본문에 쓰면
   안 되는 출처확인일/최종검토일 패턴까지 실수로 면제 목록에 넣어 새
   구멍을 만들었다. 다시 해야 할 일은 fix-verifier 보고 6개 항목 참고
   (git log 에는 없음 — 이 파일 이전 버전이나 세션 기록 참고, 또는
   approval-quality-auditor 를 다시 불러 스스로 재현).

== 에이전트 팀 (12명, .claude/agents/) ==
1c188d7 에서 8명, bec1bde 에서 handover-scribe(9번째), 1d550bb 에서
approval-quality-auditor(10번째), a7fb068 에서 fix-verifier(11번째)와
approval-readiness-designer(12번째)를 추가했다 — 전부 커밋됨(push 여부는
이 파일 맨 위 "현재 상태"의 안내대로 origin 로그로 직접 확인).
CLAUDE.md 가 AGENTS.md 를 @import 하는 구조도 1c188d7 에서 처음 생겼다.

주의: .claude/agents/ 정의는 세션(프로세스) 시작 시에만 로드된다. 세션
도중에 에이전트를 새로 추가해도 그 세션에서는 "Agent type not found" 로
호출이 실패한다 — 이번 세션에서 실제로 겪었다. claude -c 로 재시작해야
새 에이전트가 보인다.

approval-quality-auditor 를 실제로 호출해봤다 — "1년/2년" 미검증 고위험
수치 오탐 감사(위 9번). 결과는 위에 적힌 대로 fix-verifier 검증에서 실패.

사장님이 "겹치는 팀원이 있으면 안겹치게 역할을 나누던지 삭제해"라고
지시해서 12명을 전부 다시 대조했다. 실제 겹침 3곳을 찾았다:

1. docs-keeper ↔ handover-scribe — 둘 다 todo.txt 담당으로 적혀 있었다.
   docs-keeper.md 를 고쳐 todo.txt 를 handover-scribe 전담으로 넘겼다
   (이번 커밋 46dc38e 에 포함, 완료).
2. quality-root-cause ↔ approval-quality-auditor — core/quality/
   QualityEngine.ts 등 같은 파일을 둘 다 자기 담당이라 적어놨다. 아직
   안 고침.
3. quality-root-cause ↔ approval-evidence — quality-root-cause 가
   "core/ai/*.ts 전부"와 "core/approval/*Policy.ts"를 담당이라 적어놨는데
   그 파일 대부분이 이미 approval-evidence 소유다. 실제로 이 겹침 때문에
   quality-root-cause 에게 GeneratedVerifyEvidence.ts 를 고치라고 잘못
   시켰었다(결과물 자체는 문제없었지만 담당 경계가 흐렸다). 아직 안 고침.

2·3번도 커밋 70d9d0e 로 마저 고쳤다 — 12명 전부 겹침 정리 완료. 요약:
quality-root-cause 는 생성 프롬프트/스키마 + 범용 core/quality/ 엔진만,
core/ai/ApprovalSourcePreflight.ts 계열과 core/approval/*Policy.ts 는 승인
전문 3인방(approval-evidence/approval-quality-auditor/
approval-readiness-designer)에게 명시적으로 넘기게 했다.
approval-quality-auditor 는 core/quality/ 클레임을 버렸다. platform-adapter-dev
에는 apps/*/approval/ 예외 문구를 추가했다.

== 다음 할 일 ==
위 1~3번 측정은 세 번째 시도(위 7번)로 전부 끝났다 — 더 반복할 필요 없다.
남은 건 승인 게이트 쪽이다.

1. 스택된 approval-quality-auditor 수정(위 9번)을 다시 손봐야 한다.
   git stash pop 으로 꺼낸 뒤 fix-verifier 가 지적한 6개를 반영하고,
   fix-verifier 로 다시 검증받은 뒤에만 커밋한다. 지금 상태로 배포하면
   실제 외부 사실 주장(신청 자격·급여 기간·제도 개편 주기 같은 것)이
   조용히 통과되는 구멍이 있다.
2. 건강보험 피부양자 원고에 다시 "승인 준비 검사 실행"을 돌려본다. 위
   8번이 고쳐졌으니 Standard Quality 차단이 풀렸을 것이다 — 실제로
   풀렸는지, 그리고 이미 찾아둔 출처 2건이 "공식 출처와 검토 기준"
   섹션으로 원고에 실제로 붙는지 확인한다.
3. vitest 가 .claude/worktrees/ 를 스캔하는 문제(위 현재 상태 절 참고)를
   vitest.config.ts 에서 제외 설정으로 고친다.

== 보류/관찰 중인 항목 ==

- 줄바꿈(CRLF) 정리: 이번 세션 코드 diff 일부가 파일 전체의 줄바꿈을 LF→CRLF로
  바꿔 diff를 부풀렸다 (예: app/application/OpenAIProvider.ts). 저장소에
  .gitattributes 가 없어 파일마다 줄바꿈이 제각각이다. 사장님이 "3번은 병합
  후로 미뤄"라고 지시했다 — 이 브랜치가 main 에 병합된 뒤 별도
  `chore: normalize line endings` 커밋으로 처리한다. 지금 하면 진행 중인
  다른 수정과 diff가 뒤섞인다.

(GeneratedFactualClaim.ts 의 verbatim anchor 불일치 항목은 위 8번에서
고쳐졌으므로 이 절에서 제거함.)

== 따라가지 않는 것 ==
.bright-studio/ 는 git 대상이 아니다. 이번에 측정한 청약통장 원고와 품질
리포트는 옮겨지지 않는다. 다음 기계에 밝은재테크 Project 와 기존 발행 글이
있어야 반복 회피 컨텍스트와 내부 링크 카탈로그가 같은 조건으로 동작한다.
