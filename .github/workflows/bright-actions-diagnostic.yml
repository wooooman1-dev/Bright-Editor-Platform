name: Bright Actions diagnostic

on:
  pull_request:
    branches:
      - fix/wordpress-full-audit
    types:
      - synchronize

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Runner started
        run: echo "runner_started=true"
