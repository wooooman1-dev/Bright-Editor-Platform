name: Temporary Distributed Intent Fix

on:
  push:
    branches:
      - fix/canonical-table-and-image-cost-policy

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/canonical-table-and-image-cost-policy
      - name: Apply patch
        run: python scripts/temporary_distributed_intent_fix.py
      - name: Commit patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet; then
            git add core/content/ContentOpportunityAlignment.ts tests/unit/core/content/ContentOpportunityAlignment.test.ts
            git commit -m "fix: aggregate distributed search intent evidence"
            git push origin HEAD:fix/canonical-table-and-image-cost-policy
          fi
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build
