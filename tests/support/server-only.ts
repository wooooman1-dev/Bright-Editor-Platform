// Next.js resolves the real server-only package in production. Vitest runs
// these modules in a Node test environment, where the marker has no runtime
// behavior and must remain importable for server-path regression coverage.
